{
  "interval_time": {
    "description": "订阅后检测更新的时间间隔(分)",
    "type": "int",
    "default": 20,
    "hint": "例如：60"
  },
  "CWM_SUBSCRIBE_DEBUG": {
    "description": "Enable debug logging for subscription workflow.",
    "type": "bool",
    "default": false,
    "hint": "Set to true to print subscription debug logs."
  }
}
